version: '3.8'

services:
  redmushroom:
    build: .
    ports:
      - '3001:3001'
    volumes:
      - sqlite_data:/app/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET}
      - PORT=3001
    restart: unless-stopped

volumes:
  sqlite_data:
